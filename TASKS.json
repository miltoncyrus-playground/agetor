{
  "subtasks": [
    {
      "id": "trimmed-claude-md-pipeline",
      "title": "Trimmed CLAUDE.md for pipeline/child sessions (O-14)",
      "prompt": "Implement O-14 exactly as specified in PLAN.md, top to bottom. Create the new pure module src/bun/claude-md-filter.ts exporting filterClaudeMdForPipeline(text, { agentKind }) plus the AGENT_COMMAND_SHAPE_HEADING and JUBARTEAI_HEADING constants, built from the private findHeadingSection helper and the two cut* helpers described in PLAN.md sections 1a-1d (each cut fails open to a no-op on any error, per the ticket). Wire it into src/bun/orchestrator.ts per section 2: add claudeMdFilterEnabled() and resolvePipelineSystemPromptFile(claudeMdPath, agentKind, cwd) near pipelineLeanContext, and update the startTask call site (~line 1496) to compute lean = pipelineLeanContext(...) once and narrow its appendSystemPromptFile via resolvePipelineSystemPromptFile before passing it in opts.leanContext. Do NOT modify pipelineLeanContext itself, agents.ts, or CLAUDE.md's content. Add tests per section 3: src/bun/claude-md-filter.test.ts (new, covering all 5 cases listed in 3a using a fixture with headings and bullets in non-file order) and additive tests in src/bun/orchestrator-pipeline.test.ts (section 3b) covering resolvePipelineSystemPromptFile directly (filtered path differs from raw, contains only the requested kind's bullet, no JubarteAI heading; null passthrough; AGETOR_PIPELINE_CLAUDE_MD_FILTER=0 kill switch) plus a startTask-level spawn assertion for pipeline vs non-pipeline tasks. Do not touch or weaken the existing pipelineLeanContext unit tests around lines 1179-1233 in that file. Finally, append (never edit existing content) to docs/plans/pipeline-token-efficiency.md per section 5: the new table row for O-14 and the paragraph describing what shipped, exactly as drafted in the plan. Run `bun run typecheck` and `bun test src/bun/claude-md-filter.test.ts src/bun/orchestrator-pipeline.test.ts` before finishing; all must pass green.",
      "dependsOn": [],
      "acceptanceCriteria": ["AC-1", "AC-2", "AC-3", "AC-4", "AC-5", "AC-6", "AC-7", "AC-8", "AC-9", "AC-10", "AC-11"],
      "files": [
        "src/bun/claude-md-filter.ts",
        "src/bun/claude-md-filter.test.ts",
        "src/bun/orchestrator.ts",
        "src/bun/orchestrator-pipeline.test.ts",
        "docs/plans/pipeline-token-efficiency.md"
      ]
    }
  ]
}
